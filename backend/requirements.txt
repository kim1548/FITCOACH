# FITCOACH 백엔드 의존성 (Python 3.12 권장 / 3.11~3.13 가능)
#
# 설치:  pip install -r requirements.txt
# torch CPU 빌드는 아래 PyTorch 공식 인덱스에서 자동으로 받습니다.
--extra-index-url https://download.pytorch.org/whl/cpu

# --- 웹 서버 (FastAPI) ---
fastapi==0.136.1
uvicorn==0.46.0
python-multipart==0.0.27
httpx==0.28.1

# --- DB / ORM / 관리자 페이지 ---
SQLAlchemy==2.0.49
sqladmin==0.25.0

# --- 인증 (JWT, 비밀번호 해시) ---
python-jose==3.5.0
passlib==1.7.4
bcrypt==4.0.1

# --- 데이터 검증 ---
pydantic==2.13.3

# --- AI: 음식 분류 (YOLO / EfficientNet) ---
torch==2.11.0+cpu
torchvision==0.26.0+cpu
ultralytics==8.4.45

# --- 이미지 / 수치 / 데이터 처리 ---
pillow==10.2.0
numpy==1.26.4
pandas==2.1.4
